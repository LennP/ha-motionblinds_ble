connect:
  fields:
    entity_id:
      required: true
      selector:
        entity:
          domain: cover
          integration: motionblinds_ble
    connection_timeout:
      required: false
      default: 15
      selector:
        number:
          min: 0
          max: 3600
disconnect:
  fields:
    entity_id:
      required: true
      selector:
        entity:
          domain: cover
          integration: motionblinds_ble
favorite:
  fields:
    entity_id:
      required: true
      selector:
        entity:
          domain: cover
          integration: motionblinds_ble
status:
  fields:
    entity_id:
      required: true
      selector:
        entity:
          domain: cover
          integration: motionblinds_ble